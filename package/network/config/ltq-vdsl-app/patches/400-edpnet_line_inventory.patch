--- a/src/dsl_cpe_cli_access.c
+++ b/src/dsl_cpe_cli_access.c
@@ -4463,7 +4463,7 @@ DSL_CLI_LOCAL DSL_int_t DSL_CPE_CLI_G997
          break;
 #endif
       default:
-         DSL_CPE_FPrintf(out, "nReturn=-1 (interanl error)" DSL_CPE_CRLF);
+         DSL_CPE_FPrintf(out, "nReturn=-1 (internal error)" DSL_CPE_CRLF);
          return 0;
       }
    }
@@ -4473,12 +4473,17 @@ DSL_CLI_LOCAL DSL_int_t DSL_CPE_CLI_G997
 #if (INCLUDE_DSL_CPE_API_VDSL_SUPPORT == 1)
    if (DSL_CPE_CLI_CheckParamNumber(
       pCommands,
       nParamList[DSL_CPE_MAX_NE_INVENTORY_PARAM_LISTS - 2], DSL_CLI_MIN) == DSL_FALSE)
    {
+       DSL_CPE_FPrintf(out, "nReturn=-1 (Min ");
+       DSL_CPE_FPrintf(out, "%d params allowed)" DSL_CPE_CRLF, nParamList[DSL_CPE_MAX_NE_INVENTORY_PARAM_LISTS - 2]);
+
       return -1;
    }
    if (DSL_CPE_CLI_CheckParamNumber(pCommands, nParams, DSL_CLI_MAX) == DSL_FALSE)
    {
+       DSL_CPE_FPrintf(out, "nReturn=-1 (Max " );
+       DSL_CPE_FPrintf(out, "%d params allowed)" DSL_CPE_CRLF, nParams);
       return -1;
    }
 #else
